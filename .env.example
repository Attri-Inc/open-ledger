# Path to the SQLite database (default: ./data/openledger.db)
OPENLEDGER_DB=

# MCP transport: stdio (Claude Desktop/Code) or sse
MCP_TRANSPORT=stdio

# Port for SSE transport (ignored for stdio)
MCP_PORT=8791
